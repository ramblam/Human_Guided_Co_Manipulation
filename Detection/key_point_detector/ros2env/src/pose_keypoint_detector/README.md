ROS2 python package to implement pose keypoint detectors. 

- parent class `PoseKeypointDetector` with an abstract method `image_callback()`, to implement 2D keypoint detection on RGB stream 
- example of the calss usage shown in child class `Yolo11KeypointDetector`


